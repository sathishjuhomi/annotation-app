export interface FormData {
  email: string;
  password: string;
  passwordConfirm: string;
}

export interface SignUpData {
  onSubmit: any;
  formHandleSubmit: any;
  register: any;
  errors: any;
}
